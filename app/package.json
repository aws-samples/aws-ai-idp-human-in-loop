{
  "name": "app",
  "version": "0.1.0",
  "bin": {
    "app": "bin/app.js"
  },
  "scripts": {
    "cdk-app-install": "npm install --silent --save && clear && node setup.js",
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.39.1",
    "chalk": "^4.1.2",
    "enquirer": "^2.3.6",
    "jest": "^27.5.1"
  },
  "dependencies": {
    "@aws-cdk/core": "^1.193.0",
    "aws-cdk-lib": "^2.39.1",
    "constructs": "^10.0.0",
    "dotenv": "^16.0.3",
    "uuid": "^9.0.0"
  }
}
